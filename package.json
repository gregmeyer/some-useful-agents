{
  "name": "some-useful-agents",
  "version": "0.0.0",
  "private": true,
  "description": "A local-first agent playground. Author, schedule, and report on agents running on your machine.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc --build && node packages/dashboard/scripts/copy-assets.mjs && node packages/dashboard/scripts/gen-build-info.mjs && chmod +x packages/cli/dist/index.js",
    "clean": "tsc --build --clean",
    "test": "vitest run",
    "outcome:fixture": "SUA_WRITE_OUTCOME_FIXTURE=1 vitest run packages/core/src/outcome/e2e.test.ts",
    "test:watch": "vitest",
    "lint": "tsc --noEmit"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/gregmeyer/some-useful-agents.git"
  },
  "keywords": [
    "agents",
    "mcp",
    "temporal",
    "cli",
    "orchestration"
  ],
  "author": "Greg Meyer",
  "license": "MIT",
  "engines": {
    "node": ">=22.5.0"
  },
  "overrides": {
    "protobufjs": ">=7.5.5"
  },
  "devDependencies": {
    "@changesets/cli": "3.0.1",
    "vitest": "4.1.11"
  }
}
